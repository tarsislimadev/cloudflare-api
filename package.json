{
  "name": "cloudflare",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev --remote"
  },
  "devDependencies": {
    "itty-router": "^3.0.12",
    "wrangler": "^3.0.0"
  }
}